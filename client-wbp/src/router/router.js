import { App } from 'vue'

console.log(App)

export default class Router {
  constructor(options) {
    // this.routes = options.routes
    // this.components = []
    // this.current = { id: '', path: '' }
  }
}
